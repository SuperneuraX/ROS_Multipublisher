# ROS_Multipublisher
A ROS2 Humble-based multi-node system with two independent publishers and one intelligent subscriber that acts as a control unit. The subscriber dynamically switches between publishers at runtime using user input, enabling real-time topic subscription control without restarting nodes.
